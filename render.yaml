services:
  - type: static
    name: BC-Learning
    env: static
    region: oregon
    plan: free
    buildCommand: echo "No build needed for static redirect"
    publishPath: public-redirect
    envVars:
      - key: NODE_ENV
        value: production
